A hash table is a data structure that maps key to values for highly efficient lookup there are number of ways of implementing this.

first, compute the key's hash code which will usually long or int.
